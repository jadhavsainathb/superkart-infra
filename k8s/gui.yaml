apiVersion: apps/v1
kind: Deployment

metadata:
  name: gui

spec:
  revisionHistoryLimit: 2
  replicas: 1

  selector:
    matchLabels:
      app: gui

  template:

    metadata:
      labels:
        app: gui

    spec:

      containers:

      - name: gui

        image: 408502716092.dkr.ecr.ap-south-1.amazonaws.com/superkart-gui:24abd7ff9363e1a4709fb8d51dd9ed8d82715074

        imagePullPolicy: Always

        ports:

        - containerPort: 8501

---

apiVersion: v1
kind: Service

metadata:
  name: gui-service

spec:

  type: ClusterIP

  selector:
    app: gui

  ports:

  - port: 80
    targetPort: 8501